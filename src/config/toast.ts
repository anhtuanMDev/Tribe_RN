/** Scale step per depth level (0 = front/newest). */
export const DECK_SCALE_STEP = 0.06;
/** Y translation step (px) per depth level — creates the peek-behind effect. */
export const DECK_TRANSLATE_STEP = 10;
/** Opacity step per depth level. */
export const DECK_OPACITY_STEP = 0.15;
/** Fixed max visible slots. */
export const MAX_VISIBLE = 3;